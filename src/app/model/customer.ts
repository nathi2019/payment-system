import { Address } from './address';

export class Customer {
    firstName: string;
    lastName: string;
    dob_date: string;
    dob_month: string;
    dob_year: string;
    SSN: string;
    address: Address;
}